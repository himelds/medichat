[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
# Default rules: E (pycodestyle errors), F (pyflakes), W (warnings)

[tool.ruff.lint.per-file-ignores]
# (intentional pattern — global cache state must be set before module init)
"src/evaluation/run_ragas.py" = ["E402"]
